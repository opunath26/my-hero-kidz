import LoginFrom from '@/components/auth/LoginForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <LoginFrom />
        </div>
    );
};

export default page;